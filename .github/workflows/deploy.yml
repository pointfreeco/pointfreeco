name: Deploy

on:
  push:
    branches:
      - '*'
      #- master
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: jctaveras/heroku-deploy@v1.0.0
        with:
          api_key: ${{ secrets.HEROKU_API_KEY }}
          app_name: pointfreeco-stephen
          dockerfile_path: Dockerfile
          email: stephen.celis@gmail.com
